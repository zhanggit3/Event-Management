// Upload limit for the My Items library (matches the `library-files` bucket's file_size_limit).
export const MAX_LIBRARY_FILE_BYTES = 50 * 1024 * 1024; // 50 MB
export const MAX_LIBRARY_FILE_LABEL = "50 MB";
